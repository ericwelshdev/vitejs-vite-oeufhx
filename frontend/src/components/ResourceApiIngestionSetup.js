import React from 'react';
import { Typography, Container } from '@mui/material';

const ResourceApiIngestionSetup = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Resource Api Ingestion Setup</Typography>
      {/* Add dashboard content here */}
    </Container>
  );
};

export default ResourceApiIngestionSetup;